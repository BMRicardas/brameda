---
import type { Props as Link } from "./nav-link.astro";

import NavLink from "./nav-link.astro";

type Props = {
  links: Link[];
};

const { links } = Astro.props;
---

{
  links.map(({ href, text }) => (
    <li>
      <NavLink {href} {text} />
    </li>
  ))
}
